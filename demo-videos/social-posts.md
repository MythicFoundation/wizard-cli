# Wizard CLI v1.0.0 — Social Media Posts

---

## X (Twitter) Thread

### Tweet 1 (Main — attach hero demo video)

Wizard CLI v1.0.0 is live.

An AI dev agent that writes Solana programs, queries on-chain state, and manages your entire L2 — from the terminal.

Built on Claude Opus 4.6. 27 blockchain tools. Persistent memory. Agent spawning. MCP servers.

One command to install:
$ curl mythic.sh/wizard | bash

### Tweet 2

What can it do?

- Spawn specialist agents (program-engineer, validator-ops, bridge-security, defi-ops, frontend-dev)
- Write & build native Solana BPF programs (no Anchor)
- Query wallets, balances, tokens, bridge status — real RPC calls
- 6 slash commands: /build, /deploy, /audit, /supply, /bridge-status, /pool-status

### Tweet 3

Under the hood:

- Full permission system (interactive, auto, plan mode)
- Persistent memory across sessions (project + global)
- MCP server support (plug in any tool ecosystem)
- Session recording & resume
- Hook system for custom automation
- Built on @AnthropicAI Claude with streaming tool use

### Tweet 4

This is the developer toolchain for Mythic L2 — the AI-optimized Solana L2 running on Firedancer.

Every program ID, every PDA, every config path — baked into the agent's knowledge.

It doesn't guess. It checks on-chain.

github.com/MythicFoundation/wizard-cli

---

## Telegram Post (Mythic Community)

**Wizard CLI v1.0.0 — Now Available**

We just shipped the biggest upgrade to our developer toolchain. Wizard CLI is a terminal-based AI agent that writes, builds, deploys, and manages Solana programs on Mythic L2.

**What's new in v1.0.0:**
- Agent spawning — 5 specialist agents for different domains
- 27 built-in blockchain tools — real on-chain queries, not mocks
- Skills framework — /build, /deploy, /audit, /supply, /bridge-status, /pool-status
- Persistent memory — remembers your preferences, build rules, project context
- Permission system — interactive, auto, plan mode, acceptEdits, bypassPermissions
- MCP server support — connect any tool ecosystem
- Session persistence — resume conversations across sessions
- Hook system — run custom scripts before/after tool calls

**Install:**
```
curl mythic.sh/wizard | bash
```

**Source:**
github.com/MythicFoundation/wizard-cli

Built for the builders. Every Mythic L2 program ID, every PDA seed, every config path — the agent knows your infrastructure as well as you do.
